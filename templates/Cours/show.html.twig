{% extends "base.html.twig" %}

{% block body %}
    <h1 class="my-4">{{ cours.getNomCours() }}</h1>
    <div class="row">

        <div class="col-md-7">
            <p>{{ cours.getContenuCours() }}</p>

        </div>

        <div class="col-md-5">
            <img class="img-fluid rounded mb-3 mb-md-0" src="{{ cours.getImageCours() }}" alt="{{ cours.getNomCours() }}">
        </div>

        <p>Username: {{ app.user.username }}</p>


    </div>


{% endblock %}

